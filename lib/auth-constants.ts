/** HttpOnly cookie storing the CMS JWT */
export const JWT_COOKIE_NAME = "gcs_cms_token";

/** HttpOnly cookie for approved member sessions (email + member ID verified). */
export const MEMBER_JWT_COOKIE_NAME = "gcs_member_token";
